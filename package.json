{
  "name": "skill-alexa-nodejs-quote",
  "version": "1.0.0",
  "description": "An unofficial wikiquote skill",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "zip": "bestzip quote-skill.zip lib/* node_modules",
    "upload": "s3-cli --config ./aws.config --region eu-west-1 put quote-skill.zip s3://perzl-alexa/quote-skill.zip",
    "deploy": "npm run build && npm run zip && npm run upload"
  },
  "keywords": [
    "alexa",
    "skill",
    "quote",
    "wikiquote"
  ],
  "author": "Martin Perzl",
  "license": "Apache-2.0",
  "dependencies": {
    "alexa-sdk": "^1.0.15"
  },
  "devDependencies": {
    "@types/alexa-sdk": "^1.0.10",
    "bestzip": "^1.1.4",
    "s3-cli": "^0.13.0",
    "typescript": "^2.5.3"
  }
}
